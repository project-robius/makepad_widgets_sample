[package]
name = "makepad_widgets_sample"
version = "0.2.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
makepad-widgets = { path = "../makepad/widgets", version = "0.6.0" }
